function enableAutoEventTracking({
  attributeName = `tracker`,
  sourceNode = document,
  targets = [`a`, `button`],
  trackers = [`plausible`],
}) {


  return () => {

  }
}

function trackEvent({ trackers }) {

}

export { enableAutoEventTracking, trackEvent }
